public abstract class Player implements Playable {
//    add an abstract Player class that implements the Playable interface (not it's methods, however) and include the following...
//    an int constant called WORD_CADENCE set to a value of 600
//    an int constant called INTRO_PAUSE set to a value of 7000
    static final int WORD_CADENCE = 350;
    static final int INTRO_PAUSE = 7000;

}
